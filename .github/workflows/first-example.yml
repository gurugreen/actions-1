name: first
on: push
jobs:
    firstjob:
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v4
            - name: welcome
              run: echo "welcome"
            - name: list
              run: ls
            - name: read
              run: cat README.md